{
  "name": "simple-plang",
  "version": "0.1.0",
  "description": "",
  "main": "build/src/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc",
    "runindex": "yarn run build && node build/src/index",
    "main": "yarn run build && node build/src/main",
    "cli": "yarn run build && node build/src/cli",
    "bundle": "yarn run build && browserify build/src/web.js -o bundle.js",
    "start": "node build/src/cli"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/atennapel/simple-plang.git"
  },
  "author": "Albert ten Napel",
  "email": "aptennap@gmail.com",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/atennapel/simple-plang/issues"
  },
  "homepage": "https://github.com/atennapel/simple-plang#readme",
  "devDependencies": {
    "@types/node": "^14.0.27",
    "browserify": "^16.5.2",
    "typescript": "^3.9.7"
  }
}
